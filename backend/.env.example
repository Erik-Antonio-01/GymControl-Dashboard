# Copie para .env e ajuste. O arquivo .env não deve ser versionado.
PORT=3000

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=2709
DB_NAME=academia

# Troque por um valor longo e aleatório em produção.
JWT_SECRET=chave-secreta-trabalho-faculdade
